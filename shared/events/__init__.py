"""Shared event schemas — imported by producer and consumer services.

In a real polyglot architecture these would be in a separate package
published to a private PyPI registry. For this monorepo, a shared dir works.
"""
